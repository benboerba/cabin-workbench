# 个人本地版使用说明

个人本地版适合自己在一台电脑上使用：不需要注册、不需要登录，也不需要安装或配置数据库。长期习惯、个人日程、项目进度和自定义入口都会保存在一个本地文件中。

## 最快启动

需要先安装 Node.js 22 或更高版本。进入项目目录后只运行：

```bash
npm run local
```

首次运行会自动安装项目依赖，随后打开浏览器。默认访问地址是：

```text
http://127.0.0.1:4310
```

停止时回到启动窗口，按 `Ctrl+C`。

## 让 Agent 帮你启动

在项目目录中，可以直接告诉 Agent：

> 启动木屋工作台个人本地版，并告诉我访问地址。

Agent 只需运行 `npm run local`，不需要询问数据库、账号、密码或端口配置。

## 数据在哪里

默认数据文件：

```text
local-data/cabin-workbench.db
```

这是本地版的唯一业务数据文件，已被 Git 忽略，不会上传到公开仓库。需要备份时，先停止本地版，再复制整个 `local-data` 文件夹即可。

如需更换端口或数据位置：

```bash
LOCAL_PORT=4320 LOCAL_DATA_PATH=/你的路径/workbench.db npm run local
```

## 与服务器版的区别

| | 个人本地版 | 多用户服务器版 |
|---|---|---|
| 进入方式 | 直接打开，无需登录 | 邮箱和密码登录 |
| 数据位置 | 当前电脑的单个本地文件 | 服务器持久化目录 |
| 用户隔离 | 单人使用 | 每个账户独立隔离 |
| 推荐场景 | 自己使用、让 Agent 快速启动 | 分享给朋友、跨设备访问 |

服务器版继续使用 `npm run server:start`（或兼容命令 `npm start`），未设置 `APP_MODE=local` 时始终按服务器版运行。
